import React from 'react'
import "./List.css"
import Card from "../Card/Card"

function List() {
    
  return (
    <div className='list'>
        {/* { data.map(item=> (
            <Card item={ item } key={ item.id } />
        ))} */}
    </div>
  )
}

export default List;